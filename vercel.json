{
  "version": 2,
  "buildCommand": "npm install -g pnpm && pnpm install && pnpm --filter @workspace/api-server build",
  "functions": {
    "artifacts/api-server/dist/index.mjs": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "artifacts/api-server/dist/index.mjs"
    }
  ]
}